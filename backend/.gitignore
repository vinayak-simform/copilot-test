# Build output
dist/
*.js
*.js.map
!nodemon.json

# Environment variables
.env
.env.local

# TypeScript cache
*.tsbuildinfo

# Data storage (task data persists locally)
tasks.json

# Dependencies (handled by root .gitignore)
# node_modules/
# package-lock.json
